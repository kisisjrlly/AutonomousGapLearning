run_name: recipe_v3
model:
  use_risk_feedback: true
reward:
  retreat_reward_k: 0.6
  brake_k: 0.3
curriculum:
  step_up: 0.006
ppo:
  coll_horizon: 40            # risk horizon 0.5s -> 1.0s (signal early enough to act)
